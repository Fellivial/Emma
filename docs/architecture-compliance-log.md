# Emma Brain Gateway — Architecture Compliance Log

Living artifact. Appended to by every Phase 6 implementation wave (6A–6F). For every ADR requirement a wave touches, records the requirement, its governing ADR and Technical Design reference, the concrete evidence checked, a PASS/FAIL verdict, and reviewer notes. This is evidence-based — each row cites a specific file, test, or command output, not a self-report.

---

## Wave 6A — Core Infrastructure

| #   | Requirement                                                                                                                                                                 | ADR                       | Technical Design ref                              | Evidence                                                                                                                                                                                                                                                                                                                                                     | Verdict | Reviewer Notes                                                                                                                                             |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A Provider Registry exists as a per-request-queryable structure holding all configured provider instances, replacing the Gateway's single module-level provider reference   | ADR-0006                  | §3.1–§3.2                                         | `src/core/brain/registry.ts` exports `createProviderRegistry()`/`ProviderRegistry`/`RegisteredProvider`; `gateway.ts`'s `const provider: BrainProvider = createOpenRouterProvider()` is removed, replaced by `const registry = createProviderRegistry(); registry.register(...)`                                                                             | PASS    | Direct diff review confirms the module-level `const provider` no longer exists anywhere in `gateway.ts`                                                    |
| 2   | A Capabilities Descriptor is attached to each Registry entry, declaring queryable provider capabilities                                                                     | ADR-0006                  | §4.1–§4.3                                         | `CapabilitiesDescriptor` interface (`types.ts`, 6 fields); `OPENROUTER_CAPABILITIES` populated in `gateway.ts` and passed to `registry.register()`                                                                                                                                                                                                           | PASS    | Boolean-per-capability schema matches §4.1 exactly, field-for-field                                                                                        |
| 3   | The `BrainProvider` interface itself is untouched — the Registry/Descriptor sit around it, not through it                                                                   | ADR-0006                  | Context (Existing strengths preserved)            | `git diff src/core/brain/types.ts` shows zero lines changed within the `BrainProvider` interface block; only a new, separate `CapabilitiesDescriptor` interface is added above it                                                                                                                                                                            | PASS    | Confirmed by direct diff inspection, not inference                                                                                                         |
| 4   | Registration validates: no duplicate provider name; all required descriptor fields present; `contextWindowTokens > 0`                                                       | ADR-0006                  | §3.3, §4.2                                        | `registry.ts`'s `register()` throws on duplicate `provider.name`; `assertValidDescriptor()` checks all 5 boolean fields + `contextWindowTokens` is a positive integer; `tests/unit/brain-registry.test.ts` exercises all three cases directly (6 tests)                                                                                                      | PASS    | All three validation paths independently unit-tested, not merely implemented                                                                               |
| 5   | Boundary enforcement (static, CI-time): a lint rule forbidding imports of `src/core/brain/providers/*` from outside `src/core/brain/`                                       | ADR-0013                  | §11.1                                             | `eslint.config.mjs` new block: `files: ["src/**/*.ts","src/**/*.tsx"], ignores: ["src/core/brain/**"], rules: {"no-restricted-imports": [...]}` matching the Technical Design's exact snippet                                                                                                                                                                | PASS    | Rule text, `group`, and `ignores` scoping verified identical to Technical Design §11.1's own code block                                                    |
| 6   | The new lint rule does not false-positive against the current tree                                                                                                          | ADR-0013                  | §11.1                                             | `npm run lint` output: 0 errors, and zero matches on the new rule's message text anywhere in the output                                                                                                                                                                                                                                                      | PASS    | Only `gateway.ts` imports `providers/openrouter.ts` today, and it is inside the `ignores` scope                                                            |
| 7   | ADR-0003 Principle 1 (Single Inference Entry Point) remains structurally enforced, not merely conventional                                                                  | ADR-0003                  | N/A (foundational principle this wave reinforces) | All 16 Application-Layer call sites' import statements are unchanged (`grep -rl "@/core/brain/gateway\|@/lib/embeddings" src/` — same 16 files as Phase 6 Kickoff's baseline); the new Provider-boundary lint rule now makes a direct-to-provider import a CI-time failure, not merely a convention                                                          | PASS    | This wave makes Principle 1 more strongly enforced than before (tooling, not only discipline) — a strict improvement, not a change in the principle itself |
| 8   | Every "Changed" interface is additive only — no existing signature narrowed, retyped, or removed                                                                            | ADR-0006                  | Technical Design §13                              | `brainChat`/`brainChatStream`/`brainEmbed`/`isBrainConfigured` keep identical signatures (same params, same return types); `types.ts`'s only change is one new, standalone interface                                                                                                                                                                         | PASS    | Confirmed by direct diff review of both files' exported signatures                                                                                         |
| 9   | Wave 6A's "zero-behavior-change interim state" claim (task 4) actually holds                                                                                                | ADR-0006                  | Phase 5 §5.1 task 4                               | Full regression suite: 804 passed / 3 skipped / 67 of 68 files (vs. baseline 787/3/66-of-67) — 0 regressions, +17 new; stash-based rollback test independently reproduces the exact pre-6A baseline (787/3/66-of-67) with Wave 6A changes removed                                                                                                            | PASS    | Tested empirically (stash/restore), not merely asserted — see Wave 6A Implementation Report §8                                                             |
| 10  | Registry construction/registration happens synchronously at module load, identical timing to the prior module-level `const` (not a Runtime Configuration Store in disguise) | ADR-0012 (non-goal check) | §3.2, §3.5                                        | `registry.ts`/`gateway.ts` contain no `async`, no deferred registration, no add/remove method exposed after construction — `register()` is only ever called once, at `gateway.ts` module load                                                                                                                                                                | PASS    | No runtime mutation path exists; Registry remains boot-populated and immutable, matching ADR-0012's explicit non-goal for this wave                        |
| 11  | No provider-specific (OpenRouter) logic leaked outside the Provider Layer                                                                                                   | ADR-0006                  | Context (Adapter Layer boundary)                  | `registry.ts` contains zero references to OpenRouter, URLs, headers, or wire-format concepts — purely generic lookup/validation logic; the only OpenRouter-specific fact in `gateway.ts` is the `OPENROUTER_CAPABILITIES` constant, which Technical Design §3.2 itself specifies belongs at the registration call site, not inside `providers/openrouter.ts` | PASS    | Matches Technical Design's own design exactly — this is not a boundary violation, it is the specified registration pattern                                 |

**Wave 6A verdict: 11 of 11 checked requirements PASS. 0 FAIL.** No ADR-0006 or ADR-0013 requirement touched by this wave is unmet. See the [Wave 6A Implementation Report](phase6a-core-infrastructure-implementation.md) for the full narrative and the [Technical Debt Register](technical-debt-register.md) for the three items intentionally deferred to later waves (none of which represent a compliance failure — each is either explicitly scheduled in the approved plan or a documentation-precision carry-forward).
